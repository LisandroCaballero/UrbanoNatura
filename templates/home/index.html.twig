{% extends 'base.html.twig' %}

{% block title %}Urbano Natura{% endblock %}

{% block stylesheets %}
    {{ parent() }}

{% endblock %}

{% block body %}
    <nav class="navbar navbar-expand-md navbar-dark bg-dark">
        <a class="navbar-brand" href="#"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Ur Natura</font></font></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample04" aria-controls="navbarsExample04" aria-expanded="false" aria-label="Navegación de palanca">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarsExample04">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="http://example.com" id="dropdown04" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Funciones</font></font></a>
                    <div class="dropdown-menu" aria-labelledby="dropdown04">
                        <a class="dropdown-item" href="{{ assets('bajar_pi') }}"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Bajar PI </font></font></a>
                        <a class="dropdown-item" href="#"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Leer Piezas</font></font></a>
                        <a class="dropdown-item" href="#"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Impresiones RICOH</font></font></a>
                    </div>
                </li>
            </ul>
            <form class="form-inline my-2 my-md-0">
                <a href="{{ assets('logout') }}" class="btn btn-outline-danger" role="button" aria-disabled="true">Salir</a>
            </form>
        </div>
    </nav>
    <div class="container">
        {% block container %}
        {% endblock container %}
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ path('fos_js_routing_js', { callback: 'fos.Router.setData' }) }}"></script>
    <script src="{{ assets('bundles/fosjsrouting/js/router.min.js') }}"></script>
{% endblock %}
